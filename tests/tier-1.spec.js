const { expect } = require('chai')

console.clear()
describe('Tier 1: Getting Started', () => {
  describe('waitForSignal', () => {
    it('is called', () => {
      // throw new Error('didnt get called')
    })
  })
})
